# Admin Panel Backend

- View all batches (both plans)
- Override batch status
- Retry failed leads manually
- View system runtime
- Control dispatcher (start/stop)

Access: Only specific UID allowed (hardcoded for now)
